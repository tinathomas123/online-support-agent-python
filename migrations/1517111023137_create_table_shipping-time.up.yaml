- args:
    sql: CREATE TABLE "shipping-time"("order-status" text NOT NULL, "shipping-time-in-days"
      integer NOT NULL, PRIMARY KEY ("order-status") )
  type: run_sql
- args:
    name: shipping-time
  type: add_existing_table_or_view
